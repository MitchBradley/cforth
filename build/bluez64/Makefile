# Builds a Linux host Forth application with serial port tools and Bluetooth tools with Nod device support

TOPDIR=../..

CONFIG += -DBITS64
LIBS += -lpthread

include $(TOPDIR)/src/app/bluez/targets.mk
