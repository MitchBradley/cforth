Use ../host-serial-linux/Makefile as an example for how to set up
a build directory for a host-resident CForth.

Use ../arm-xo-1.75/Makefile as an example for how to set up
a build directory for an embedded CForth.
